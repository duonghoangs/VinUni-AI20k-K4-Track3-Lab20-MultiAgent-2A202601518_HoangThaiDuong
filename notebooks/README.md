# Notebooks

Optional area cho học viên tạo notebook demo. Production code vẫn nên nằm trong `src/`, không viết toàn bộ logic trong notebook.

## Nội dung

- `demo_multi_agent_walkthrough.ipynb` — notebook hoàn chỉnh hướng dẫn từng bước: shared state → mock services → demo agents → supervisor routing → mini workflow → benchmark. Notebook chạy không cần API key và có bảng mapping sang implementation production trong `src/multi_agent_research_lab/`.
